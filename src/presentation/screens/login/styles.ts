import { StyleSheet } from 'react-native';

const loginStyles = StyleSheet.create({
  container: {
    flex: 1,
  },

  linear:{
    position: 'absolute',
    left: 0,
    right: 0,
    top: 0,
    zIndex: -1
  },
  
})

export default loginStyles;